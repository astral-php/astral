Options -Indexes
RewriteEngine On

# Redirige toutes les requêtes vers index.php
# sauf si le fichier/répertoire existe physiquement
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
